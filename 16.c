#include<stdio.h>
void main()
{
int low,high,i,f,temp;
printf("enter two numbers");
scanf("%d %d",&low,&high);
if(high<low)
{
temp=low;
low=high;
high=temp;
}
printf("%d %d",low,high);
while(low<high)
{
f=0;
break;
}
}
if(f==0)
printf("%d:,low);
++low;
}
}

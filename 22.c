#include<stdio.h>
int main()
{
int array[100],minutes,size,c,location=1;
printf("enter the number of element in array \n");
scanf("%d",&size);
printf("enter %d integers \n",size);
for(c=0;c<<size;c++)
scanf("%d",&array[c]);
minumum=array[0];
f0r(i=1;1<=N;I++)
{
  if(maximum<array[i])
  {
    maximum=array[i];
  }
}
  printf("%d",maximum);
  return 0;
}
